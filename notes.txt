let a = new Uint8ClampedArray( [ 1, 2, 3 ] );
let b = new Uint8ClampedArray( [ 4, 5, 6 ] );

let c = new Uint8ClampedArray(a.length + b.length);
c.set(a);
c.set(b, a.length);

console.log(a);
console.log(b);
console.log(c);

let imgData = new ImageData(c, width, height);